/**
 * Maak 4 verschillende types van variabelen aan.
 * 2 van de variabelen zijn aanpasbaar doorheen de rest van de code,
 * 2 van de variabelen zijn NIET meer aanpasbaar.
 */

let woord = "hallo"; //variabele dat wel nog kan veranderen
let getal = 15; //variabele dat wel nog kan veranderen
const PI = 3.14; //variabele dat niet meer kan veranderen
const dier = "hond"; //variabele dat niet meer kan veranderen
